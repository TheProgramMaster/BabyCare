/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package web.build.app;

import web.build.list.LinkedList;

import static web.build.utilities.StringUtils.join;
import static web.build.utilities.StringUtils.split;
import static web.build.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
